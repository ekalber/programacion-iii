<strong>Imprimo el objeto request</strong>: 
<%= request.inspect%>
<br>
<strong>Método y formato request:</strong>
<br>
<strong>Método:</strong> <%= request.method%>
<strong>Formato:</strong> <%= request.format %>
<br>
<strong>Parámetros:</strong>
<%= params.inspect %>
<br>
<strong>Imprimo el objeto response:</strong>
<%= response.inspect %>
<br>
<strong>Estado:</strong> <%= response.status %>
<br>

<p id="notice"><%= notice %></p>
<h1>Authors</h1>

<table>
  <thead>
    <tr>
      <th><%= link_to "First name", authors_path(:sort => true, :by => "first_name", :order => (params[:order] == "asc" ? "desc" : "asc")) %></th>
      <th><%= link_to "Last name", authors_path(:sort => true, :by => "last_name", :order => (params[:order] == "asc" ? "desc" : "asc")) %></th>
      <th>Libros</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @authors.each do |author| %>
      <tr>
        <td><%= author.first_name %></td>
        <td><%= author.last_name %></td>
        <td>
          <% if author.books.count == 0 %>
            <%= author.books.count %>
          <% else %>
            <%= link_to author.books.count, books_path(:author_id => author.id) %>
          <% end %>
        </td>
        <td><%= link_to 'Show', author %></td>
        <td><%= link_to 'Edit', edit_author_path(author) %></td>
        <td><%= link_to 'Destroy', author, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Author', new_author_path %>
